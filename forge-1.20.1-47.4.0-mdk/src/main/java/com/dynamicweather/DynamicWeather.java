package com.dynamicweather;

import net.minecraftforge.fml.common.Mod;

@Mod("dynamicweather")
public class DynamicWeather {

    public DynamicWeather() {
        // This constructor is run during mod loading.
        // You can use it to register event listeners, but it's safe to leave it empty for now.
    }
}
